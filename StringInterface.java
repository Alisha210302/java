// Define the StringInterface with a method to apply a transformation to a string
public interface StringInterface {

    // Method that takes a string and returns a transformed string
    String apply(String s);
}
